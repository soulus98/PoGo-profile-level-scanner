const config = require("../config.json");
const prefix = config.chars.prefix;

module.exports = {
	name: "force-quit",
	description: "Forcibly closes the bot server. IDK why you'd need to do this but I've included it just in case.",
  aliases: ["quit","end"],
  usage: `${prefix}quit`,
	guildOnly:true,
	permissions: "ADMINISTRATOR",
	execute(message, args) {
    
	},
};
