// Need to make a new version based on role granting


// const messagetxt = require("../server/messagetxt.js"),
// 			{ messagetxtReplace } = require("../func/misc.js");
//
// module.exports = {
// 	respondVerify(message){
// 		if (message.embeds[0]) {
// 			if (message.embeds[0].title == ":white_check_mark: Complete") {
// 				const taggedUser = message.mentions.users.first();
// 				message.channel.send(messagetxtReplace(messagetxt.respondVerify, taggedUser));
// 			}
// 		}
// 	},
// };
